# Day-8---Python-Challenge-Pair-Target-Difference-
This repository is about,Day 8 – Pair Target Difference  Pure Python solution using a frequency map (dictionary) to count unique pairs with an absolute difference of "k". Handles duplicates and the "k = 0" edge case. Runs in O(n) time and O(n) auxiliary space, without nested loops or brute force.
